<%= form_with(model: ciudade, local: true) do |form| %>
  <% if ciudade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ciudade.errors.count, "error") %> prohibited this ciudade from being saved:</h2>

      <ul>
      <% ciudade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :estado_id %>
    <%= form.text_field :estado_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
