<%= form_with(model: paise, local: true) do |form| %>
  <% if paise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(paise.errors.count, "error") %> prohibited this paise from being saved:</h2>

      <ul>
      <% paise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :codigo %>
    <%= form.text_field :codigo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
